import QuasianalyticLean.DenjoyCarleman.DC45

/-!
# Denjoy–Carleman uniqueness: components (Bang's argument, discrete chain)

* `DC1_ratio_pow`, `DC2_bang_step` — log-convexity bound and Bang's step (`DC12.lean`).
* `DC3_chain` — the chain from a flat point (`DC3.lean`).
* `DC4_forward`, `DC5_backward` — forward uniqueness and reflection (`DC45.lean`).
-/
